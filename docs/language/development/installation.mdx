---
title: Installation
icon: Download
description: How to begin developing with Sabre
---

import { Callout, Install } from '@/website/components';

Sabre ships as a single, dependency-free executable. It can be installed through the scripts below for Linux, Darwin and Windows.

<Install />

To check that Sabre was installed successfully, open a new terminal window and run:

```shell title="Terminal"
sabre --version # should output "vx.y.z-build"
```

<Callout type="warning">
    **Note**: If you receive a `command not found` error, you may have to manually add the installation directory
    (`~/.sabre/bin`) to your `$PATH`.
</Callout>

<Callout>
    **Building Natively**: Sabre can be built natively by following the [instructions available
    here](/project/development).
</Callout>

## Upgrading / Removing

Once installed, the Sabre binary can upgrade or remove itself:

```shell title="Terminal"
sabre upgrade --tag latest  # upgrades to the latest version
sabre upgrade --remove      # removes Sabre from your system
```

<Callout>

    **Note**: To manually remove Sabre:

    ```shell tab="Linux / Darwin"
    rm -rf ~/.sabre
    ```

    ```batch tab="Windows"
    rmdir /s /q $HOME\.sabre
    ```

</Callout>
