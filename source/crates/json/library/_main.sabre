///
/// @crate "json"
/// @describe Helpers for parsing and serializing JSON/JSONC.
///

// Dynamic Modules
import "dylib:json" as json;

// Sabre Modules
import "sabre:_detail" as Detail;

// - HELPERS - //

//! @brief Internal helper to reduce bytecode output.
let m_cast = Detail.Unsafe.cast;

// - PUBLIC METHODS - //

//! @brief Stringifies Sabre values into a JSON output.
//! @param value                Value to stringify.
export let encode = m_cast[fn (value: Any) -> String](json.encode);

//! @brief Parses JSON inputs to a Sabre value.
//! @param input                Input to parse.
export let decode = m_cast[fn [T = Any](input: String) -> T](json.decode);
